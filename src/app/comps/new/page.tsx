import { ComingSoon } from "@/components/Placeholder";

export default function NewCompPage() {
  return (
    <ComingSoon
      title="Add a Comp"
      description="This page will capture a past underwritten deal: property and market, the team's A/B/C/D/GE category, Crow's position in the stack, loan terms (amount, rate/spread, term, IO), credit metrics (LTV, LTC, DSCR, debt yield), sponsor, and outcome — plus a one-motion import of the Multifamily OM Index workbook. The data model is already built and waiting underneath."
    />
  );
}
