import { ComingSoon } from "@/components/Placeholder";

export default function NewDealAnalysisPage() {
  return (
    <ComingSoon
      title="New Deal Analysis"
      description="This page will take a new opportunity (the subject deal), classify it into the team's categories (existing mezz / stretch senior / construction / ground-up equity), and screen it against the comp database using the five-filter discipline: location (zip, then 3-mile radius), property type, vintage ±3 years, occupancy ±10 points, and category matching — ending in a side-by-side comparison with range bars showing where the subject sits."
    />
  );
}
